#include <stdio.h>
int main() {
    int a = 12, b = 8, c = 2, d = 3;
    a /= b++ -c * d;  //여기서 b++은 없다고 봐도돼 바로 증가해서 적용된값을 넣는게 아냐. 일단 원래 b값 먼저 넣고.. 후위증가연산자이기때문이다.
    printf("%d", a);
}

// /=: 대입연산자다 산술연산자 아님 그러므로 제일 마지막에 계산한다.
// 대입연산자는 제일 마지막에 하는거다!!
// 앞에 /,*,+,-,%(나머지 연산)올 수 있다. =